input {
  dead_letter_queue {
    path => "/path/to/data/dead_letter_queue_dir"
    # Leave commented out to just read / review DLQ
    # commit_offsets => true 
    pipeline_id => "main"
  }
}

output {
  stdout {
    codec => rubydebug { metadata => true }
  }
}